 "use client"; import {useState,FormEvent} from "react";import {createClient} from "@/lib/supabase/client";
export default function Login(){const s=createClient();const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");async function go(x:FormEvent){x.preventDefault();setErr("");const{error}=await s.auth.signInWithPassword({email:e,password:p});if(error)setErr(error.message);else location.href="/dashboard"}return <main className="login"><form className="loginbox" onSubmit={go}><div className="brand" style={{color:"#111827",padding:"0 0 8px"}}>PROPUBLIC</div><p className="muted">Sistema de gestión interna</p>{err&&<div className="error">{err}</div>}<label className="field">Email<input type="email" value={e} onChange={x=>setE(x.target.value)} required/></label><br/><label className="field">Contraseña<input type="password" value={p} onChange={x=>setP(x.target.value)} required/></label><br/><button className="btn" style={{width:"100%"}}>Ingresar</button></form></main>}