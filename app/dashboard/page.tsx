import Shell from "@/components/Shell";import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";
export default async function Dashboard(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');return <Shell title="Dashboard"><h1 className="title">Panel principal</h1><p className="muted">Resumen operativo de ProPublic Industria Publicitaria.</p><div className="grid">{[['Clientes','0'],['Presupuestos','0'],['Pedidos activos','0'],['Ventas','Gs. 0']].map(x=><div className="card" key={x[0]}><div className="muted">{x[0]}</div><div className="metric">{x[1]}</div></div>)}</div><div className="panel"><h2>Sesión</h2><p className="muted">{user.email}</p></div></Shell>}